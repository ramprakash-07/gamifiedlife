name: lifeisgame
description: Personal OS v2.0 — Cyberpunk productivity suite.
publish_to: 'none'
version: 3.0.0+3

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  sqflite: ^2.3.2
  path: ^1.9.0
  home_widget: ^0.6.0
  fl_chart: ^0.66.0
  flutter_local_notifications: ^17.0.0
  excel: ^4.0.0
  share_plus: ^7.2.2
  intl: ^0.19.0
  path_provider: ^2.1.2
  timezone: ^0.9.2
  flutter_timezone: 5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
